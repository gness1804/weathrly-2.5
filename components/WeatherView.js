import React, { Component } from 'react';
import { ScrollView, Text } from 'react-native'

class WeatherView extends Component {
  render() {
    return (
      <ScrollView>
        <Text>I am the WeatherView component.</Text>
      </ScrollView>
    );
  }
}

export default WeatherView
