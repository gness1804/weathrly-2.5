import React, { Component } from 'react';
import { View, Text } from 'react-native'

class Header extends Component {
    render() {
        return (
            <View>
                <Text>I am the header component.</Text>
            </View>
        );
    }
}

export default Header;