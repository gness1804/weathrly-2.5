import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    marginTop: 30,
  },
  degreeReadout: {
    fontSize: 26,
  },
  headline: {
    fontSize: 20,
    fontWeight: '600',
    marginBottom: 20,
  },
})

export default styles
